- defaults:
      link:
          create: true
          relink: true

- clean:
      ~:
          force: true
      ~/.config:
          recursive: true

- link:
      ~/.p10k.zsh: home/.p10k.zsh
      ~/.zshenv: home/.zshenv
      ~/.config/:
          glob: true
          path: config/*
          exclude: [config/Code]
      ~/.local/share/fonts/:
          glob: true
          path: fonts/*

- shell:
      - [git submodule update --init --recursive, Installing submodules]
